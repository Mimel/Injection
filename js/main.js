// Generated by CoffeeScript 2.4.1
(function() {
  (function($) {
    var InjectionRunner, Node, commandDictionary, runnertest;
    commandDictionary = {
      // Goes to the following node, carrying a value.
      // Expects 2 parameters, a node, and a value.
      inject: function(values) {
        return console.log('Hello!');
      },
      // Returns to the node's parrent, carrying the value of the field indicated.
      // Expects 1 parameter, a field.
      return: function(values) {},
      // Sets a field in the node to the value indicated.
      // Expects 2 parameters, a field and a value.
      set: function(values) {}
    };
    Node = class Node {
      constructor(name, fields, linksTo) {
        this.name = name;
        this.fields = fields;
        this.linksTo = linksTo;
      }

      displayName() {
        return console.log(this.name);
      }

    };
    InjectionRunner = class InjectionRunner {
      constructor(code) {
        this.code = code;
      }

      runLine(line) {
        var lexemes;
        lexemes = line.split(' ');
        return commandDictionary[lexemes[0]](function() {
          return lexemes.slice(1);
        });
      }

    };
    runnertest = new InjectionRunner('inject 3').runLine('inject 3');
    $(function() {
      return console.log('We are live!');
    });
    return test.displayName();
  })(jQuery);

}).call(this);
